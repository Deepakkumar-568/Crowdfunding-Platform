version: '3.8'

services:
  db:
    image: mysql:8.0
    container_name: crowdfunding-db
    environment:
      MYSQL_ROOT_PASSWORD: rootpassword
      MYSQL_DATABASE: crowdfunding_db
    ports:
      - "3306:3306"
    volumes:
      - mysql-data:/var/lib/mysql

  ollama:
    image: ollama/ollama:latest
    container_name: crowdfunding-ollama
    ports:
      - "11434:11434"
    volumes:
      - ollama-data:/root/.ollama

  backend:
    build:
      context: ./Antigravity.Backend
      dockerfile: Dockerfile
    container_name: crowdfunding-backend
    ports:
      - "7165:8080"
    environment:
      - ASPNETCORE_ENVIRONMENT=Development
      - ConnectionStrings__DefaultConnection=Server=db;Database=crowdfunding_db;User=root;Password=rootpassword;
      - Jwt__Secret=CrowdfundingSuperSecretKeyForJWTSignaturesPlaceholder2026
      - Jwt__Issuer=Crowdfunding
      - Jwt__Audience=CrowdfundingUsers
      - Ollama__BaseUrl=http://ollama:11434
    depends_on:
      - db
      - ollama

  frontend:
    build:
      context: ./Antigravity.Frontend
      dockerfile: Dockerfile
    container_name: crowdfunding-frontend
    ports:
      - "4200:80"
    depends_on:
      - backend

volumes:
  mysql-data:
  ollama-data:
